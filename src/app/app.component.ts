import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  // template: `<app-new></app-new>`
  template: `<app-default-layout></app-default-layout>`
})
export class AppComponent {
  title = 'rocker';
}
